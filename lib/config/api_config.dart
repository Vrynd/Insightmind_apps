/// API Configuration
/// This file is now safe to commit to version control as sensitive keys 
/// have been removed. Chat functionality now uses a local dataset.
class ApiConfig {
  // Add other public API keys here if needed
  // static const String otherApiKey = 'YOUR_API_KEY_HERE';
}
